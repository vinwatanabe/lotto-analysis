export type LottoMaxType = {
  product: string;
  drawNumber: number;
  sequenceNumber: number;
  drawDate: string;
  numbers: [number, number, number, number, number, number, number];
  bonusNumber: number;
};
